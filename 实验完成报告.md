# Spring Boot 文件上传实验完成报告

## 一、完成的功能清单

### 1. 多模块项目配置 ✅
- **shop-common/pom.xml**: 添加了 `spring-boot-starter-web` 和 `spring-boot-configuration-processor` 依赖
- **shop-web/pom.xml**: 已正确依赖 shop-common
- **shop-admin/pom.xml**: 已正确依赖 shop-common
- **启动类配置**: WebApplication 和 AdminApplication 都已配置 `@ComponentScan("com.wzc")` 和 `@MapperScan`

### 2. 基础版文件上传 ✅
- **配置类**: `FileUploadProperties.java` - 读取yml中的自定义配置
- **WebMvcConfig.java**: 配置静态资源映射 `/uploads/**` → `file:uploads/`
- **FileServiceImpl.java**: 实现文件上传核心逻辑
- **测试页面**: `shop-web/src/main/resources/templates/upload.html`
- **控制器**: `UploadController.java`

### 3. 安全验证 ✅
已实现三层验证：
- **文件大小验证**: 超过10MB拒绝
- **文件类型验证**: 仅允许 `image/jpeg`, `image/png`, `image/gif`, `image/bmp`, `image/webp`
- **文件头魔数验证**: 检测文件真实类型，防止文件伪装

### 4. 图片缩放 ✅
使用 Thumbnailator 库自动生成多尺寸：
- **原图**: 保留原始尺寸
- **PC版 (_pc)**: 800x800，保持宽高比
- **手机版 (_mobile)**: 400x400，保持宽高比

### 5. 文件去重 ✅
- 使用 MD5 哈希值识别相同文件
- 数据库表 `wzc_file_record` 存储文件记录
- 上传相同文件时返回已存在的URL，不重复保存

### 6. shop-web 用户头像上传 ✅
- **个人中心页面**: `profile.html`
- **ProfileController.java**: 处理头像上传和个人信息更新
- **UserService**: 提供 `updateProfile()` 和 `updateAvatar()` 方法

### 7. shop-admin 商品管理 ✅
- **商品列表页面**: `admin/product/list.html`
- **商品表单页面**: `admin/product/form.html`（支持添加和编辑）
- **ProductPageController.java**: 处理商品CRUD操作
- **功能**: 添加、编辑、上下架、逻辑删除

### 8. 富文本编辑器集成 ✅
- 使用 wangEditor v5
- 支持图片上传到服务器
- 编辑商品时可回显已有内容

---

## 二、测试方法

### 启动项目

```bash
# 启动 shop-web（端口8080）
cd shop-web
mvn spring-boot:run

# 启动 shop-admin（端口8081）
cd shop-admin
mvn spring-boot:run
```

### 测试页面

| 功能 | 访问地址 |
|------|----------|
| 文件上传测试 | http://localhost:8080/upload |
| 个人中心 | http://localhost:8080/profile |
| 商品管理列表 | http://localhost:8081/admin/product/list |
| 添加商品 | http://localhost:8081/admin/product/add |

### 测试步骤

1. **测试基础文件上传**
   - 访问 http://localhost:8080/upload
   - 选择一张图片上传
   - 查看上传结果，验证原图、PC版、手机版URL

2. **测试安全验证**
   - 尝试上传 `.php`、`.exe` 等非图片文件
   - 应该被拦截并提示"文件类型不允许"

3. **测试图片缩放**
   - 上传一张大图
   - 检查 `uploads/` 目录下是否生成了 `_pc` 和 `_mobile` 版本

4. **测试文件去重**
   - 上传同一张图片两次
   - 第二次应该提示"文件已存在（去重）"

5. **测试用户头像**
   - 访问 http://localhost:8080/profile
   - 上传新头像，修改昵称等信息
   - 点击保存

6. **测试商品管理**
   - 访问 http://localhost:8081/admin/product/list
   - 点击"添加商品"，填写信息并上传图片
   - 在商品详情中使用富文本编辑器上传图片
   - 测试编辑、上架/下架、删除功能

---

## 三、文件结构

```
shop-common/
├── src/main/java/com/wzc/common/
│   ├── config/
│   │   ├── FileUploadProperties.java  (新增)
│   │   ├── WebMvcConfig.java          (修改)
│   │   └── MyBatisPlusConfig.java
│   ├── entity/
│   │   ├── FileRecord.java
│   │   ├── Product.java
│   │   └── User.java
│   ├── mapper/
│   │   ├── FileRecordMapper.java
│   │   ├── ProductMapper.java
│   │   └── UserMapper.java
│   └── service/
│       ├── FileService.java
│       ├── ProductService.java
│       ├── UserService.java
│       └── impl/
│           ├── FileServiceImpl.java    (包含安全验证、缩放、去重)
│           ├── ProductServiceImpl.java
│           └── UserServiceImpl.java

shop-web/
├── src/main/java/com/wzc/web/
│   ├── WebApplication.java
│   └── controller/
│       ├── UploadController.java       (新增)
│       ├── ProfileController.java      (新增)
│       ├── FileController.java
│       └── UserController.java
└── src/main/resources/
    ├── application.yml
    ├── application-dev.yml             (添加文件上传配置)
    └── templates/
        ├── upload.html                 (新增)
        └── profile.html                (新增)

shop-admin/
├── src/main/java/com/wzc/admin/
│   ├── AdminApplication.java
│   └── controller/
│       ├── ProductPageController.java  (新增)
│       ├── ProductController.java
│       └── FileController.java
└── src/main/resources/
    ├── application.yml
    ├── application-dev.yml             (添加文件上传配置)
    └── templates/
        └── admin/product/
            ├── list.html               (新增)
            └── form.html               (新增)
```

---

## 四、API接口

### shop-web (端口8080)

| 接口 | 方法 | 说明 |
|------|------|------|
| /upload | GET | 显示上传页面 |
| /upload | POST | 处理文件上传 |
| /profile | GET | 显示个人中心 |
| /profile/update | POST | 更新个人信息和头像 |
| /web/file/upload | POST | API方式上传文件 |
| /web/file/upload/avatar | POST | 上传头像 |

### shop-admin (端口8081)

| 接口 | 方法 | 说明 |
|------|------|------|
| /admin/product/list | GET | 商品列表页面 |
| /admin/product/add | GET | 添加商品页面 |
| /admin/product/save | POST | 保存新商品 |
| /admin/product/edit/{id} | GET | 编辑商品页面 |
| /admin/product/update | POST | 更新商品 |
| /admin/product/status/{id}/{status} | GET | 修改商品状态 |
| /admin/product/delete/{id} | GET | 逻辑删除商品 |
| /admin/file/upload | POST | 上传文件 |
| /admin/file/upload/editor | POST | 富文本编辑器上传图片 |

---

## 五、注意事项

1. **数据库**: 确保 MySQL 已创建 `shop_web` 和 `shop_admin` 数据库，并执行 `init.sql`

2. **上传目录**: 首次运行会自动创建 `uploads/` 目录

3. **文件大小限制**: 默认单文件最大 10MB，可在 `application-dev.yml` 中修改

4. **支持的图片格式**: jpg, jpeg, png, gif, bmp, webp

5. **富文本编辑器**: 使用 wangEditor v5，通过 CDN 加载，需要联网
