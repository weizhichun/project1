# 在线商城项目说明

## 项目概述

本项目包含两个阶段的开发内容：
- **阶段1**：原生Servlet登录项目
- **阶段2**：Spring Boot前后端分离电商项目

---

## 阶段1：原生Servlet登录项目

### 项目结构
```
login-web/
├── index.html              # 登录页面
└── WEB-INF/
    ├── web.xml           # Web配置文件
    └── classes/
        └── com/
            └── servlet/
                └── LoginServlet.java   # 登录Servlet
```

### 功能说明
1. 登录页面：用户名密码输入框，美观UI
2. 固定账号：admin / 123456
3. 登录成功/失败提示

### 部署说明
需要部署到Tomcat 10+，使用Jakarta包

---

## 阶段2：Spring Boot电商项目

### 项目结构
```
shop-web/
├── src/main/
│   ├── java/com/wzc/web/
│   │   ├── controller/
│   │   │   └── api/
│   │   │   │   ├── TestApiController.java      # API测试接口
│   │   │   │   ├── UploadApiController.java       # 文件上传API
│   │   │   │   ├── ProductApiController.java       # 商品API
│   │   │   │   └── UserApiController.java          # 用户API
│   │   │   └── ... (原有Controller)
│   └── resources/
│       ├── static/
│       │   ├── index.html           # 前后端分离首页
│       │   ├── api-test.html        # API测试页面
│       │   └── ajax-upload.html     # 文件上传页面
│       └── templates/ (原有模板)
```

### API接口说明

#### 1. API测试接口
- `GET /api/test/hello` - 测试GET请求，支持name参数
- `POST /api/test/user` - 测试POST请求，JSON格式

#### 2. 文件上传接口
- `POST /api/upload` - 文件上传，返回原图、PC版、手机版URL

#### 3. 商品接口
- `GET /api/product/list?page=1&size=6` - 商品分页列表
- `GET /api/product/{id}` - 商品详情

#### 4. 用户接口
- `POST /api/user/login` - 用户登录
- `POST /api/user/logout` - 退出登录
- `GET /api/user/current` - 获取当前登录用户

### 访问地址

| 页面 | 地址 | 说明
|------|------|-----
| 前后端分离首页 | http://localhost:8080/index.html | 商品列表、登录
| API测试页面 | http://localhost:8080/api-test.html | 测试API接口
| 文件上传页面 | http://localhost:8080/ajax-upload.html | 测试文件上传

### 启动说明

1. 启动shop-web项目（端口8080）
2. 默认已有账号：admin / 123456，user / 123456
3. 访问上述页面进行测试

---

## 安全改进记录

### 已修复的安全问题
1. ✅ DebugController添加@Profile("dev") - 仅在开发环境启用
2. ✅ 用户信息接口添加权限验证 - 只能查看自己的信息
3. ✅ 购物车操作添加用户验证 - 只能操作自己的购物车
4. ✅ 配置安全Session配置http-only和same-site - 增强安全

---

## 项目运行

- 包名：com.wzc
- 端口：8080(前台) / 8081(后台)
- 数据库：H2（内存数据库

